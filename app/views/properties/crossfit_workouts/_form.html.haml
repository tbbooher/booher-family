= simple_form_for @crossfit_workout, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :description
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), crossfit_workouts_path, :class => 'btn'
