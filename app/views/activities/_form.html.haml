= simple_form_for(@activity) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :location_id
    = f.input :description
    = f.input :trip_day_id

  .form-actions
    = f.button :submit
