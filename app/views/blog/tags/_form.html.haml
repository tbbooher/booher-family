= simple_form_for [:blog, @tag], :html => { :class => 'form-horizontal' } do |f|
  = f.input :title
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), blog_tags_path, :class => 'btn'
