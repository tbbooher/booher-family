= simple_form_for @goal, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :due_date
  = f.input :description
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), goals_path, :class => 'btn'
